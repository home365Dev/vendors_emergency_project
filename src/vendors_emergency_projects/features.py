# import src.vendors_emergency_projects.config
# from feature_operators.feature_operator_factory import FeatureOperatorFactory
#
# factory = FeatureOperatorFactory()
#
#
# ###
# def _create_features_vec(raw_df):
#     # to_keep = [0 for i in cat_vars]
#     # return to_keep
#
#     for feature in FeatureOperatorFactory.cat_vars:
#         factory.run_feature_operator(feature, raw_df)
#
#
# def run(raw_df):
#     _create_features_vec(raw_df)
#     # raw_df[config.FEATURES] = raw_df[config.TFIDF_TITLE].apply(lambda str: _create_features_vec(str))

